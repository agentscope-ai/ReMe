"""AgentScope integration namespace for Meta-ReMe.

The package is named after AgentScope's conventional ``as`` directory. Since
``as`` is a Python keyword, callers should normally load it through
``importlib.import_module('as.agent')`` or import the modules after adding the
``meta-reme`` directory to ``sys.path``.
"""
